#ReplyMate ✉️🤖

AI-powered email reply generator paste an email, pick a tone, get a polished reply in seconds.

ReplyMate uses LLaMA 3.3 70B (via Groq) to draft context-aware email replies, wrapped in a Spring Boot backend and a React frontend, fully containerized and deployed.
